# one Agent Notes

- Keep command behavior safe for local monorepo workflows.
- Use `os/exec` for git operations and surface useful errors.
- Favor explicit flags for file paths so commands can run from any subdirectory.
